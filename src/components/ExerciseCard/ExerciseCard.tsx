import { FC } from "react";

type Props = {
  exercise: string;
};

export const ExerciseCard: FC<Props> = ({ exercise }) => {
  return (
    <div>
      {/*  */}
      ExerciseCard
      {/*  */}
    </div>
  );
};
